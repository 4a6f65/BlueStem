.panel.panel-primary
    .panel-heading.text-center New Courses
    .panel-body
        .row(ng-repeat="course in courses | orderBy:'published':true | limitTo:15")
            .col-md-3 {{course.published | date:'MMM d yyyy'}}
            .col-lg-9 {{course.name}}
